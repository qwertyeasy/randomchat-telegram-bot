Сейчас код уже делится на слои, но есть один технический момент: message.bot["redis"] и message.bot["db_session"] в таком виде ещё надо правильно прокинуть через startup-зависимости. Это нормально, следующий шаг — сделать dependency wiring в main.py и общий контейнер зависимостей.
Также matcher пока упрощённый: он уже работает как ядро, но потом я бы усилил его блокировкой гонок и отдельной транзакционной логикой, чтобы два /find не создали двойную сессию.